var sum int = 0;
var i int = 1;

while true {
  var j int = 1;

  while true {
    sum = sum + i * j;
    j = j + 1;
    if j > 5 { break; }
  }

  i = i + 1;
  if i > 5 { break; }
}

printf("%d\n", sum);
